bundle.vim
==========

plugin for vim

$ git clone https://github.com/wyaocn/bundle.vim.git ~/.vim
$ git submodule init
$ git submodule update

$ ln -s ~/.vim/vimrc ~/.vimrc

install ctags
install ack-grep

Launch vim and run :VundleInstall
